package com.alim.taskflow.core.theme

import androidx.compose.ui.graphics.Color

// Primary Colors
val Blue = Color(0xFF007AFF)
val Purple = Color(0xFF5856D6)

// Status Colors
val Red = Color(0xFFFF3B30)
val Orange = Color(0xFFFF9500)
val Green = Color(0xFF34C759)
val Gray = Color(0xFF8E8E93)

// Light Theme Colors
val LightBackground = Color(0xFFFFFFFF)
val LightSurface = Color(0xFFF2F2F7)
val LightSurfaceVariant = Color(0xFFE5E5EA)
val LightOnBackground = Color(0xFF000000)
val LightOnSurface = Color(0xFF000000)

// Dark Theme Colors
val DarkBackground = Color(0xFF000000)
val DarkSurface = Color(0xFF1C1C1E)
val DarkSurfaceVariant = Color(0xFF2C2C2E)
val DarkOnBackground = Color(0xFFFFFFFF)
val DarkOnSurface = Color(0xFFFFFFFF)

// Gradient Colors
val GradientStart = Blue
val GradientEnd = Purple

// Priority Colors
val PriorityLow = Blue
val PriorityMedium = Orange
val PriorityHigh = Red

// Status Colors
val StatusTodo = Gray
val StatusInProgress = Orange
val StatusDone = Green